---
# defaults file for replace
os_name: "{{ ansible_distribution }}"

is_debug: false
is_backup: true
debug_value: "{{ '- True' if is_debug else '- False' }}"

type: "oneline"
filename: "NULL"
regexp_word: "NULL"
replace_word: "NULL"
